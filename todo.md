# TODO

1. Shorten the text in the navigation by introducing "title: replacement text"
entries alongside the "- file: index.md" sort of entries.

2. Add abbreviations with fly-over explantions for words that are
unfamiliar; for example, `Erntetage (mandatory harvesting work)`.
This will display only **Erntetage** with .... underneath it, and when one
hovers over the text, **mandatory harvesting work** will display.

